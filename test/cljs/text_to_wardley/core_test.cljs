(ns text-to-wardley.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [text-to-wardley.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 1))))
